# Runtime-only dependencies for the Streamlit demo.
# Install with: pip install -r demo/requirements.txt
# PyTorch must be installed separately (see README).

monai[all]==1.3.0
streamlit==1.33.0
matplotlib==3.8.4
numpy==1.26.4
nibabel==5.2.1
torch==2.2.2
